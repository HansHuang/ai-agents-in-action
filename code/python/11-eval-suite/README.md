# 11 — Eval Suite

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

This chapter contains evaluation tooling and test harnesses. Install the
requirements in a dedicated virtual environment before running the suite.
