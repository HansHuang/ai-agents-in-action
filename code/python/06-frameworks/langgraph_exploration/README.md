# 07 — LangGraph Exploration

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

This chapter explores language graph patterns in Python. Activate the
virtual environment before running any demos or tests.
