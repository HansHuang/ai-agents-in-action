"""Entry point — runs the prompt template example.

See prompt_template.py for the full implementation.
For the few-shot and chain-of-thought demos run those scripts directly.
"""
from prompt_template import main

if __name__ == "__main__":
    main()
