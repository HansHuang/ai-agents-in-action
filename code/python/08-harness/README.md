# 08 — Harness

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

This chapter demonstrates harness patterns for routing, validation, and
fallback logic. Use the same virtual environment for repeatable results.
